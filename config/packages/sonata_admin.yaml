sonata_admin:
    title: 'Portfolio Admin'
    dashboard:
        blocks:
            - { type: sonata.admin.block.admin_list, position: left }
        groups:
            Default:
                label:                Default
                label_catalogue:      ~
                items:
                - admin.project

sonata_block:
    blocks:
        sonata.admin.block.admin_list:
            contexts: [admin]
